# models

Store exported model artifacts, serialized weights, configuration snapshots, and MLflow links in this folder. Keep large binaries out of version control and version the metadata that documents the best-performing runs.

