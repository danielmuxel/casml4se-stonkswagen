# data

Use this folder to store raw and intermediate datasets required by experiments. Keep large or sensitive files out of version control and prefer documenting download or generation steps inside `scripts/` or `docs/`.

